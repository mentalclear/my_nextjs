// A file that imports Global CSS files. 
// The only location to be used for that purpose
import '../styles/global.css'


export default function App({ Component, pageProps }) {
    return <Component {...pageProps} />
}